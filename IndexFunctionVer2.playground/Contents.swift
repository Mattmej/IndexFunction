//: Playground - noun: a place where people can play

import UIKit

var str = "Hello, playground"

/*
 
 Goal: create function that returns index of a string of letters that fits a predetermined string. The index will be of the letter that comes first in the predetermined string.

 */

func findIndex(str:String, myStr:String) -> Int {
//    var myStringLength = myStr.count
    
    
//    for i in 0..<str.count-myStr.count {
//        let currentStr = str[i + (str.count-1)]
//    }
}

extension String {
    var substring: String {
        let range = Range(uncheckedBounds: <#T##(lower: _, upper: _)#>)
    }
}
